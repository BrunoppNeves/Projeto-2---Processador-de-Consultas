enum Endpoint {
  HELLO_WORLD = '/helloWorld',
  VERIFY_QUERY = '/verifyQuery',
  CONVERT_QUERY = '/convertQuery',
}

enum Paths {
  ApiRoute = '/api',
  ApiPort = 8080,
}

export { Endpoint, Paths };
